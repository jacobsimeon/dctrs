<div class="row providers">
  <table class="span12 table-striped table-bordered table-hover">
    <tbody>
      <% @providers.each do |p| %>
        <tr class="provider">
          <td> <%= p.name.first rescue "" %> </td>
          <td> <%= p.name.last rescue ""%> </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<div class="row">
